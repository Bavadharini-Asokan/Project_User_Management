<div class="container" style="margin-top:50px;">
  <%= link_to "<button class='btn btn-primary pull-right buttons_style'>Back</button>".html_safe, users_path %>
  <h3>Projects</h3>
  <% @projectuser.each do |project| %>
    <%= Project.find(project.project_id).project_name %></br></br>
  <% end %>
  <h3>Related Department</h3>
  <%= @department %>
</div>

<style>
  .buttons_style{
    width:100px;
    background-color: white;
    border-color: #007bff;
    margin-left:10px;
    color: #007bff;
  }
  .buttons_style:hover{

      background-color: #007bff;

  }
</style>
